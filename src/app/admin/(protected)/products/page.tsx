import { getAdminOverview } from '@/server/admin-overview';
export default async function AdminProducts() { const data = await getAdminOverview(); return <><header className="admin-title"><div><span className="eyebrow">COMMERCIAL ACCESS</span><h1>اشتراک و API</h1><p>پیش‌نیازهای فروش، مصرف API و وضعیت دسترسی‌ها.</p></div></header><div className="admin-grid"><article className="admin-card"><h2>اشتراک</h2><strong>{new Intl.NumberFormat('fa-IR').format(data.activeSubscriptions)}</strong><p>اشتراک فعال</p><p>فعال‌سازی خودکار فقط بعد از تأیید پرداخت از سمت سرور انجام می‌شود.</p></article><article className="admin-card"><h2>API</h2><strong>{new Intl.NumberFormat('fa-IR').format(data.activeApiKeys)}</strong><p>کلید فعال</p><p>کلیدها فقط به‌صورت هش در دیتابیس نگهداری می‌شوند و سهمیهٔ روزانه دارند.</p></article></div><article className="admin-card wide"><h2>وضعیت فروش</h2><ul><li>مدل‌های محصول و دورهٔ اشتراک در دیتابیس وجود دارد.</li><li>صدور کلید، درگاه پرداخت و تأیید خرید اپ هنوز باید به سرویس پرداخت منتخب وصل شوند.</li><li>تا تکمیل تأیید پرداخت، فروش را در رابط عمومی فعال نکنید.</li></ul></article></> }
