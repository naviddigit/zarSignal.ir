import { test,expect } from '@playwright/test';
test('market board is honest, searchable and keeps favorites',async({page})=>{await page.goto('/');await expect(page.locator('html')).toHaveAttribute('dir','rtl');await expect(page.getByRole('heading',{name:'نبض بازار'})).toBeVisible();await expect(page.locator('.source-disclosure')).toContainText('قیمت نمایشی حذف شده است');await expect(page.locator('.market-table tbody tr')).toHaveCount(6);await page.getByRole('textbox',{name:'جست‌وجوی بازار'}).fill('دلار');await expect(page.locator('.market-table tbody tr')).toHaveCount(1);await page.getByRole('button',{name:'نشان‌کردن دلار آمریکا'}).click();await page.reload();await page.getByRole('button',{name:'منتخب',exact:true}).click();await expect(page.locator('.market-table tbody tr')).toHaveCount(1)});
test('calculator starts empty and theme offers all three modes',async({page})=>{await page.goto('/');const calculator=page.locator('.calculator-machine');await expect(calculator.locator('input')).toHaveCount(3);for(const input of await calculator.locator('input').all())await expect(input).toHaveValue('');for(const label of ['سیستم','روشن','تیره'])await expect(page.getByRole('button',{name:label,exact:true})).toBeVisible();await calculator.locator('input').nth(0).fill('10');await calculator.locator('input').nth(1).fill('8500000');await expect(calculator.locator('strong')).toContainText('۸۵٬۰۰۰٬۰۰۰')});
test('no invented bubbles and private API requires credentials',async({page,request})=>{await page.goto('/');await expect(page.locator('.bubble-value')).toHaveCount(3);await expect(page.locator('.bubble-value').first()).toContainText('در انتظار');const publicResponse=await request.get('/api/public/markets');expect(publicResponse.status()).toBe(200);expect((await publicResponse.json()).quotes).toEqual([]);const privateResponse=await request.get('/api/v1/quotes');expect(privateResponse.status()).toBe(401);expect(privateResponse.headers()['cache-control']).toBe('no-store');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=window.innerWidth)).toBe(true)});
test('information routes and sitemap render',async({page,request})=>{for(const path of ['/pricing','/developers','/mobile','/methodology','/risk-management']){await page.goto(path);await expect(page.getByRole('heading',{level:1})).toBeVisible()}expect((await request.get('/sitemap.xml')).status()).toBe(200)});
